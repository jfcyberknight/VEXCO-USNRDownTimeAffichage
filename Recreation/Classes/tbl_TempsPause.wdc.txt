tbl_TempsPause est une Classe
    m_uuid est un chaîne // Identifiant unique stable (UUID)
    m_IDTypePause est un entier
    m_fld_DescriptionPause est un chaîne
    m_fld_Duree est un entier

FIN

PROCÉDURE Constructeur()
// Initialisation du UUID si nouveau
SI m_uuid = "" ALORS m_uuid = DonneGUID(guidFormatDashed)

PROCÉDURE Destructeur()

PROCÉDURE ChargerParUUID(sUUID)
SI HLitRecherchePremier(tbl_TempsPause, uuid, sUUID) ALORS
    FichierVersMémoire(objet, tbl_TempsPause)
    RENVOYER Vrai
FIN
RENVOYER Faux

PROCÉDURE Enregistrer()
MémoireVersFichier(objet, tbl_TempsPause)
RENVOYER HEnregistre(tbl_TempsPause)
