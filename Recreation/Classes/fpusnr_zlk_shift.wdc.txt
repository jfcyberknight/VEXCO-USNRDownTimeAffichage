fpusnr_zlk_shift est une Classe
    m_uuid est un chaîne // Identifiant unique stable (UUID)
    m_ShiftNumber est un entier
    m_ShiftName est un chaîne
    m_PLCShiftNumber est un entier

FIN

PROCÉDURE Constructeur()
// Initialisation du UUID si nouveau
SI m_uuid = "" ALORS m_uuid = DonneGUID(guidFormatDashed)

PROCÉDURE Destructeur()

PROCÉDURE ChargerParUUID(sUUID)
SI HLitRecherchePremier(fpusnr_zlk_shift, uuid, sUUID) ALORS
    FichierVersMémoire(objet, fpusnr_zlk_shift)
    RENVOYER Vrai
FIN
RENVOYER Faux

PROCÉDURE Enregistrer()
MémoireVersFichier(objet, fpusnr_zlk_shift)
RENVOYER HEnregistre(fpusnr_zlk_shift)
