tbl_PasTempsArret est une Classe
    m_uuid est un chaîne // Identifiant unique stable (UUID)
    m_fld_IDPasUnArret est un entier
    m_fld_DescPasUnArret est un chaîne

FIN

PROCÉDURE Constructeur()
// Initialisation du UUID si nouveau
SI m_uuid = "" ALORS m_uuid = DonneGUID(guidFormatDashed)

PROCÉDURE Destructeur()

PROCÉDURE ChargerParUUID(sUUID)
SI HLitRecherchePremier(tbl_PasTempsArret, uuid, sUUID) ALORS
    FichierVersMémoire(objet, tbl_PasTempsArret)
    RENVOYER Vrai
FIN
RENVOYER Faux

PROCÉDURE Enregistrer()
MémoireVersFichier(objet, tbl_PasTempsArret)
RENVOYER HEnregistre(tbl_PasTempsArret)
